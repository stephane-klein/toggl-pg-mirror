TOGGL_PG_MIRROR_TOGGL_API_TOKEN="..." # ...

TOGGL_PG_MIRROR_ADMIN_TOKEN="$(openssl rand -hex 32)"

TOGGL_PG_MIRROR_MCP_READER_POSTGRES_PASSWORD="$(openssl rand -base64 24)" # MCP read-only role password (creates toggl_mcp_reader at startup)

SMTP_PASS="..." # SMTP password (optional, Mailpit has no auth)
